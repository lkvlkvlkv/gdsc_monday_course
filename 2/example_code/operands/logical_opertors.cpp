#include <iostream>
using namespace std;

int main() {
    bool a = true, b = false;
    
    bool logicalAnd = a && b;  // false
    bool logicalOr = a || b;   // true
    bool logicalNot = !a;      // false

    return 0;
}