#include <iostream>
using namespace std;

int main() {
    int a = 3, b = 5;

    bool isEuqual = a == b;          // false
    bool isNotEqual = a != b;        // true
    bool isGreaterOr = a > b;        // false
    bool isLessOr = a < b;           // true
    bool isGreaterOrEqual = a >= b;  // false
    bool isLessOrEqual = a <= b;     // true

    return 0;
}